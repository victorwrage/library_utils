package org.bitbucket.dollar;

import java.util.Calendar;

/**
 * Possible Date fields which map to constants in {@link Calendar}.
 * 
 * @author Adam L. Davis
 */
public enum DateField {

    SECOND(Calendar.SECOND), MINUTE(Calendar.MINUTE), HOUR(Calendar.HOUR), DAY(
            Calendar.DAY_OF_YEAR), MONTH(Calendar.MONTH), YEAR(Calendar.YEAR);

    public final int calendarField;

    private DateField(int calendarField) {
        this.calendarField = calendarField;
    }

}
